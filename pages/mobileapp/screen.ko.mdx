# 화면

## 화면 설정
## JavaScript 섹션

## 방향

### 가로 모드로 화면 시작

화면을 가로 모드로 만들려면.

1. 변수 저장
Jevil.save('ORIENTATION', 'landscape')

2. '-landscape'가 포함된 가로 모드 sketch(또는 figma) 준비

그런 다음 Jevil.go('screen')을 실행하면 변수 'ORIENTATION' == 'landscape'를 감시하여 자동으로 가로 모드로 표시됩니다 